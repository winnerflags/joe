# CUIGG Reporting Tool — pinned, offline-friendly dependencies.
streamlit>=1.33
pandas>=2.0
plotly>=5.20
python-docx>=1.1
vaderSentiment>=3.3.2
# kaleido 0.2.1 bundles its own Chromium so static PNG export works fully offline.
# (kaleido >=1.0 requires a system Chrome install.)
kaleido==0.2.1
